import React from 'react';

const TaskCard = ({task, index}) => {

    return (
        <div className='border border-[var(--border)] rounded relative'>
            <h2 className=''>{'Heading'}</h2>
        </div>
    );
};

export default TaskCard;